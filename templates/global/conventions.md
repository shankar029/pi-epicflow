# Global Conventions

> Always/never rules that apply across **every** repo you work on.
> Lives at `~/.pi/global-memory/conventions.md`.
>
> Append-only. Cite by `GC-NNN` (Global Convention) to distinguish
> from per-repo `C-NNN`. When a per-repo convention contradicts a
> global one, per-repo wins and the agent surfaces the conflict.

**Last verified:** {{DATE}}

---

<!-- Entry shape — inside a code fence so audits skip it.

```md
## GC-NNN — <short rule>

**Date:** YYYY-MM-DD
**Status:** active | superseded (by GC-NNN)
**Scope:** all-repos | language:<lang> | runtime:<runtime>

**Rule:**
<the rule, one or two sentences. e.g. "Always run the project's lint
target locally before committing. Never rely on CI to catch lint
failures.">

**Why:**
<one or two sentences. Why this is a rule and not a suggestion.>

**Exceptions:**
- <"vendored / generated code is exempt">
- <"throwaway scripts in /tmp">

**Related:** GD-NNN, C-NNN (if a per-repo convention specializes this)
```

-->

<!-- ===== Real entries below ===== -->
